{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q 'git worktree add' && echo '{\"decision\":\"block\",\"reason\":\"Use /zuggie:wt instead of git worktree add directly.\"}' || true"
          }
        ]
      }
    ]
  }
}
